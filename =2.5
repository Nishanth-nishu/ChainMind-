ERROR: pip's dependency resolver does not currently take into account all the packages that are installed. This behaviour is the source of the following dependency conflicts.
chainmind 0.1.0 requires aiofiles>=24.1.0, which is not installed.
chainmind 0.1.0 requires aiosqlite>=0.20.0, which is not installed.
chainmind 0.1.0 requires chromadb>=0.5.0, which is not installed.
chainmind 0.1.0 requires fastapi>=0.115.0, which is not installed.
chainmind 0.1.0 requires google-generativeai>=0.8.0, which is not installed.
chainmind 0.1.0 requires grpcio>=1.66.0, which is not installed.
chainmind 0.1.0 requires grpcio-health-checking>=1.66.0, which is not installed.
chainmind 0.1.0 requires grpcio-reflection>=1.66.0, which is not installed.
chainmind 0.1.0 requires grpcio-tools>=1.66.0, which is not installed.
chainmind 0.1.0 requires numpy>=1.26.0, which is not installed.
chainmind 0.1.0 requires rank-bm25>=0.2.2, which is not installed.
chainmind 0.1.0 requires sentence-transformers>=3.0.0, which is not installed.
chainmind 0.1.0 requires structlog>=24.4.0, which is not installed.
chainmind 0.1.0 requires uvicorn[standard]>=0.30.0, which is not installed.
chainmind 0.1.0 requires websockets>=13.0, which is not installed.
