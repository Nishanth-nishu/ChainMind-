# =============================================================================
# ChainMind — Environment Configuration (Local LLM Only)
#
# ChainMind runs exclusively on local vLLM servers.
# Gemini and Ollama have been removed.
# =============================================================================

# =============================================================================
# LOCAL vLLM (PRIMARY — REQUIRED)
# =============================================================================
LOCAL_VLLM_ENABLED=true
LOCAL_VLLM_BASE_URL=http://localhost:8100/v1
LOCAL_VLLM_MODEL=Qwen/Qwen2.5-7B-Instruct
LOCAL_VLLM_SERVED_NAME=chainmind-qwen
LOCAL_VLLM_GPU_MEMORY_UTIL=0.90
LOCAL_VLLM_MAX_MODEL_LEN=16384
LOCAL_VLLM_PREFIX_CACHING=true

# LLM router — local only
LLM_PRIMARY_PROVIDER=local
LLM_FALLBACK_CHAIN=["local"]
LLM_MAX_RETRIES=3
LLM_TIMEOUT_SECONDS=60
LLM_CIRCUIT_BREAKER_THRESHOLD=5
LLM_CIRCUIT_BREAKER_RECOVERY_SECONDS=60

# =============================================================================
# OPENAI (OPTIONAL — research baseline comparison only)
# Uncomment and fill in your key only when running GPT-4 baseline experiments
# =============================================================================
# OPENAI_API_KEYS=sk-...
# OPENAI_MODEL=gpt-4o

# =============================================================================
# HuggingFace Cache
# =============================================================================
HF_HOME=/scratch/nishanth.r/hf_cache
# HF_TOKEN=hf_...   # Required for gated models (Llama-3.1)

# =============================================================================
# API Server
# =============================================================================
API_HOST=0.0.0.0
API_PORT=8000
API_WORKERS=4
API_RATE_LIMIT_PER_MINUTE=60
API_KEY=changeme-in-production

# =============================================================================
# gRPC Server
# =============================================================================
GRPC_HOST=0.0.0.0
GRPC_PORT=50051
GRPC_MAX_WORKERS=10

# =============================================================================
# Retrieval / RAG
# =============================================================================
EMBEDDING_MODEL=all-MiniLM-L6-v2
CHROMADB_PERSIST_DIR=./data/chromadb
RETRIEVAL_TOP_K=10
RERANKER_TOP_K=5
RETRIEVAL_MODE=hybrid

# =============================================================================
# Memory
# =============================================================================
MEMORY_SHORT_TERM_MAX_TOKENS=4000
MEMORY_LONG_TERM_DB_PATH=./data/memory.db

# =============================================================================
# Guardrails
# =============================================================================
GUARDRAILS_ENABLED=true
MAX_INPUT_LENGTH=10000
MAX_OUTPUT_TOKENS=4096
MAX_AGENT_STEPS=10

# =============================================================================
# Observability
# =============================================================================
LOG_LEVEL=INFO
LOG_FORMAT=json
TRACING_ENABLED=true
METRICS_ENABLED=true

# =============================================================================
# Deployment
# =============================================================================
ENVIRONMENT=development
DEBUG=false
